pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                //git url: 'https://github.com/bstcoudray/repo-dev-python', branch: 'main'
                checkout scm
            }
        }
    }
}
